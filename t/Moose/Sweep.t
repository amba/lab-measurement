#!perl

use warnings;
use strict;
use 5.010;
use lib 't';
use Test::More;
use Lab::Test import => ['file_ok'];

# use Lab::Moose;
# use Lab::Moose::Sweep;

# use File::Temp qw/tempdir/;

# my $dir = tempdir();

# my $inst = instrument(type => 'DummySource',
#                       connection_type => 'Debug'
# my $master = Lab::Moose::Sweep->new(
ok(1);
done_testing();
